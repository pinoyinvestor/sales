{
  "name": "sales-mcp",
  "version": "1.0.0",
  "description": "Sales MCP Server for Claude Code — email outreach, tracking, and CRM tools",
  "type": "module",
  "main": "dist/index.js",
  "directories": {
    "doc": "docs"
  },
  "scripts": {
    "build": "tsup src/index.ts --format esm --clean --external zod --external @modelcontextprotocol/sdk --external better-sqlite3 --external imapflow --external nodemailer --external hono --external @hono/node-server --external uuid",
    "dev": "tsup src/index.ts --format esm --watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@hono/node-server": "^1.19.11",
    "@modelcontextprotocol/sdk": "^1.27.1",
    "better-sqlite3": "^12.8.0",
    "hono": "^4.12.8",
    "imapflow": "^1.2.16",
    "nodemailer": "^8.0.3",
    "uuid": "^13.0.0"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "@types/nodemailer": "^7.0.11",
    "@types/uuid": "^11.0.0",
    "tsup": "^8.5.1",
    "typescript": "^5.9.3"
  }
}
